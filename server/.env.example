# === Supabase Config ===

# Your Supabase project URL
SUPABASE_URL=https://your-supabase-url.supabase.co

# Supabase service role key (server-side only — DO NOT expose this in frontend)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# JWT secret (used for signing/verifying tokens, if applicable)
JWT_SECRET=your-jwt-secret-here

# Frontend URL allowed to make requests to your backend
ALLOWED_ORIGIN=https://your-frontend.com

# Port your backend will run on locally
PORT=port-number
